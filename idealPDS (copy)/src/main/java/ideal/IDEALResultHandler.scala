package ideal

import boomerang.WeightedForwardQuery
import boomerang.results.ForwardBoomerangResults
import wpds.impl.Weight

class IDEALResultHandler[W <: Weight] {
  def report(seed: Nothing, res: Nothing): Unit = {
  }
}